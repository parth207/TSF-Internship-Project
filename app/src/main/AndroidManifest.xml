<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EducationalDetails" />
        <activity android:name=".ProfessionalDetails" />
        <activity android:name=".PersonalDetails" />
        <activity android:name=".PublicView" />
        <activity android:name=".ProfDetails" />
        <activity android:name=".EduDetails" />
        <activity android:name=".PerDetails" />
        <activity android:name=".Certificate" />
        <activity android:name=".Certi" />
        <activity android:name=".Utils.ImageUtil" />
        <activity android:name=".P1Details" />
    </application>

</manifest>